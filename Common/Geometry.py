def is_pythagorean_triple(a: int, b: int, c: int) -> bool:
    """ This function checks the input a, b, c to see whether they satisfy the equation a^2 + b^2 = c^2 """
    return True if (a**2 + b**2) == c**2 else False
